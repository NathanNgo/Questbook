version: "2"

linters:
  default: standard

formatters:
  enable:
    - gofumpt
    - goimports
    - swaggo
  settings:
    gofumpt:
      extra-rules: true
    goimports:
      local-prefixes: github.com/NathanNgo/Questbook/backend